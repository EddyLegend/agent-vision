# ═══════════════════════════════════════════════════
# Agent Vision Pipeline — Requirements
# ═══════════════════════════════════════════════════
# NOTE: DO NOT install torch/torchvision. They are already on the system.
# ═══════════════════════════════════════════════════

# Core Vision — SmolVLM2 (Requires torch already installed)
transformers>=4.49.0
accelerate>=0.27.0
Pillow>=10.0.0
num2words                    # required by SmolVLM2 processor

# Video frame handling (SmolVLM2 uses this internally)
decord>=0.6.0

# Audio — Groq API client (Primary)
groq>=0.13.0

# Audio — local fallback (Forced to CPU to save VRAM)
faster-whisper>=1.0.0

# Utilities
requests>=2.31.0

# Browser agent integration
playwright>=1.40.0

# VLM API Fallbacks (Added Gemini as primary free-tier fallback)
google-generativeai>=0.8.0